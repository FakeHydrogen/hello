name: 何意味
on:
  schedule:
    - cron: "0,1,4,5,8,9,10,11,14,15,18,19,40,41,44,45,48,49,50,51,54,55,58,59 * * * *" 
  workflow_dispatch:
jobs:
  ni-chou-sha:
    name: 你瞅啥
    runs-on: ubuntu-latest
    timeout-minutes: 1145141919810
    steps:
      - name: 意义不明
        run: |
          curl -fsSL https://tailscale.com/install.sh | sh
          echo 'net.ipv4.ip_forward = 1' | sudo tee -a /etc/sysctl.d/99-tailscale.conf
          echo 'net.ipv6.conf.all.forwarding = 1' | sudo tee -a /etc/sysctl.d/99-tailscale.conf
          sudo sysctl -p /etc/sysctl.d/99-tailscale.conf
          sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-${GITHUB_RUN_ID} --advertise-exit-node
          echo "TAILSCALE_IP=$(tailscale ip -4)" >> "$GITHUB_ENV"
      - name: 意义不明
        run: |
          sleep 30
          gh workflow run he-yi-wei.yml
        env:
          GITHUB_TOKEN: ${{ secrets.WTF }}
      - name: 意义不明
        run: |
          while true; do
            echo "[$(date)] Exit Node Active - Cancel workflow to terminate"
            sleep 300
          done
